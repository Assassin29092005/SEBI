# Reference DRHPs

Place 2–3 real filed SME DRHPs here (public documents from BSE SME / NSE
Emerge). They are used **only** for:

1. the coverage benchmark (`backend/app/coverage.py`), and
2. section-structure reference.

They are **not** templates to copy text from — the boilerplate detector
flags near-duplicates of these filings for exactly that reason.
