export default function DraftViewer() {
  // TODO (day 5–7): render generated sections; every sentence carries a
  // clickable citation back to its source fact; [REQUIRES INPUT] markers
  // visually distinct.
  return (
    <section>
      <h1 className="text-2xl font-semibold mb-2">Draft DRHP</h1>
      <p className="text-gray-600">
        Your draft, section by section — every statement traceable to its
        source.
      </p>
    </section>
  );
}
