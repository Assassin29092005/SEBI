# LLM provider keys — at least one required. Client picks the first configured provider.
GEMINI_API_KEY=
GROQ_API_KEY=

# Which provider to prefer: "gemini" | "groq"
LLM_PROVIDER=gemini

# Backend
API_HOST=127.0.0.1
API_PORT=8000
